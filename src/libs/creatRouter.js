import { homeMenuRoutes,errorPage,permissionsMenu } from '@/router/routes'

export const createSides =  (permissions) => {
    return homeMenuRoutes
}
export const createRoutes =  (permissions) => {
    return permissionsMenu
}
